<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="Borders.Base" TargetType="Border">
        <Setter Property="Padding" Value="16 16 16 16"/>
        <Setter Property="CornerRadius" Value="8 8 8 8"/>
    </Style>
    
    <Style x:Key="Borders.Primary" TargetType="Border" BasedOn="{StaticResource Borders.Base}">
        <Setter Property="Background" Value="{DynamicResource Brushes.BackgroundPrimary}"/>
    </Style>

    <Style x:Key="Borders.Secondary" TargetType="Border" BasedOn="{StaticResource Borders.Base}">
        <Setter Property="Background" Value="{DynamicResource Brushes.BackgroundSecondary}"/>
    </Style>

    <Style x:Key="Borders.Tertiary" TargetType="Border" BasedOn="{StaticResource Borders.Base}">
        <Setter Property="Background" Value="{DynamicResource Brushes.BackgroundTertiary}"/>
    </Style>
    
</ResourceDictionary>